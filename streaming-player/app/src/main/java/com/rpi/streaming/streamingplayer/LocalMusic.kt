package com.rpi.streaming.streamingplayer

class LocalMusic(bSelected: Boolean, categoryDirectory: String, fileName: String) {
    var bSelected: Boolean? = bSelected
    var categoryDirectory: String? = categoryDirectory
    var fileName: String? = fileName
}
